# AGENTS.md — 医学AI日报项目规则

> 本文件定义“医学 AI 日报”独立项目的协作规范，对助理（Agent）和用户同样生效。
> 任何流程变更请先更新本文件，再执行。
> 本目录是完整、独立的项目边界，不依赖上级目录中的规则、脚本或数据文件。

## 1. 项目定位

**医学AI日报（Medical AI Daily）**

- 用途：每日沉淀医学人工智能领域的动态，聚焦"AI + 医疗健康"交叉点。
- 范围：医学影像、临床决策支持、医疗大模型、医学机器人、医疗Agent、AI 生物安全与合规等。
- 形态：**`index.html`** 固定展示最新一期；旧首页按自身日期归档为 `daily/YYYY-MM-DD.html`，均为纯静态、零依赖 HTML。
- 视觉基准：`template.html` 是只读设计模板；日常生成必须先读取并严格沿用，只有人工升级模板时才允许修改。
- 收录边界：必须同时具备"医学/临床"与"AI/计算"双重属性；纯医学（无 AI）或纯 AI（无医学应用）不收录。**严禁硬塞通用 AI 条目**（如通用世界模型、Agent 插件规范、编码工具等非医疗内容）。
- 原则：行业动态以当日或近期信息为主；论文研究限定在执行日前 2-3 个月内，严格最多回溯 90 天。在该时间窗内始终坚持**领域匹配优先于具体发布时间、质量优先于数量、宁缺毋滥**（目标 15-25 条，无合格内容的研究分段或版块直接省略）；不得为了凑数继续扩大到更早论文。

## 2. 独立项目边界

- 本项目仅管理 `MedAI/` 目录内的文件，并作为独立 Git 仓库发布。
- 最新公开入口是本目录 `index.html`；历史日报保存在 `daily/`，生成新一期前先归档旧首页。
- 动态关键词直接维护在本文件第 6.3 节，不再读取父目录规则。

## 3. 目录结构

```
MedAI/
├── AGENTS.md           # 项目规则、数据源与页面模板规范
├── template.html       # 只读视觉与 DOM 基准；不承载当日真实数据
├── index.html          # 最新一期公开首页；每天原位更新
└── daily/              # 历史日报；按旧首页自身日期归档
    └── YYYY-MM-DD.html
```

## 4. 最新首页与历史归档管理

- `index.html` 始终代表最新一期，文件名不随日期改变；新首页的 `<title>`、Hero 和更新时间必须更新为本次执行日期。
- `daily/` 只保存已下线的旧首页，归档名使用旧页面自身显示的日报日期：`daily/YYYY-MM-DD.html`。不得用新页面日期或文件修改时间冒充旧日报日期。
- 生成新一期前，必须先完整读取当前 `index.html` 并提取其日报日期；只有旧页成功归档后，才允许更新 `index.html`。
- 旧页日期与本次新首页日期不同时才执行日归档；同日重复生成视为修订当前一期，直接更新 `index.html`，不产生重复日归档。
- 若 `daily/YYYY-MM-DD.html` 已存在：内容与当前旧首页完全一致则跳过重复写入；内容不同则保留已有文件，并将待归档旧页写为 `daily/YYYY-MM-DD-HHmmss.html`，时间后缀使用北京时间。若带后缀路径仍重名，重新取得北京时间生成唯一文件名；仍无法避免冲突时停止归档，任何情况下都禁止覆盖历史内容。
- 无法从旧首页可靠提取日期时，停止更新并报告，不得猜测日期、不得先覆盖 `index.html`。
- 归档页必须保留旧首页的完整 HTML/CSS/JS、医学免责声明与原有内容，不得改写为新一期，也不得归档 `template.html`。
- 除 `daily/` 外禁止新增其他归档目录；日期命名 HTML 只能位于 `daily/`，不得放在项目根目录。
- 文件使用 UTF-8 编码、LF 行尾；样式与脚本全部内联。
- 修改前必须完整读取 `template.html` 与当前 `index.html`；以模板的 DOM、CSS、响应式和无障碍约束为准，只替换当日内容与统计，并保持医学免责声明完整。
- `template.html` 不属于每日产物，不得把模板占位符直接留在 `index.html`，不得在每日任务中修改模板。

## 5. 日报内容规范

### 5.1 版块（仅两个，顺序固定）

1. **论文研究** 📄：重点期刊正式论文、Online First / Article in Press 论文与 ArXiv 医学 AI 预印本；版块内部必须继续按第 6.2 节的固定研究点和第 6.3 节的研究关键词分段，不得按期刊拆版块，也不得把所有论文混排成一个卡片列表
2. **行业动态** 📊：医疗 AI 政策/合作/融资/安全事件/产品发布（来自 aihot）

> 注意：不做模型发布/更新、产品发布/更新、技巧与观点等拆分，仅上述两个版块。

论文研究的二级分段顺序为：固定研究点在前，研究关键词在后；仅展示本期有合格论文的分段。每篇论文只归入一个最匹配的主分段，并按 DOI、PMID、期刊文章号、ArXiv ID 与规范化标题联合去重。同一研究同时存在期刊版与预印本时优先收录期刊版，不重复展示 ArXiv 版本。

### 5.2 条目格式（固定字段）

每条卡片固定包含：

- **序号**：全局连续编号（跨版块不重新计数）
- **标题**：一句话概括，建议不超过 30 字
- **研究分段**：论文必须标识所属固定研究点或研究关键词，并显示对应分段标签；行业动态不使用此字段
- **来源 chip**：
  - 期刊论文 → `期刊简称 · 大学/研究机构名`（如 `TMI · 约翰斯·霍普金斯大学`、`MIA · 中科院深圳先进院`）；ArXiv 预印本 → `ArXiv · 大学/研究机构名`。大学或研究机构是必填信息，必须从作者 affiliations、论文首页/正文或可靠的一手页面核验；无法确认至少一个机构的论文不收录
  - 多机构合作论文优先标识第一作者、通讯作者或牵头团队所属机构；可列 1-3 个关键机构，不得根据作者姓名或邮箱域名主观猜测
  - 行业动态 → 原始来源名（如 `Anthropic Newsroom`、`OpenAI 官网`）
- **摘要**：**完整中文摘要，不截断**；所有英文论文摘要均须基于原文翻译/改写为中文，不直接粘贴英文 abstract
- **发布日期**：北京时间人话格式（今天/昨天/N天前；较早论文用 YYYY年M月D日），**不展示 ISO 字符串**；指信息发布日期（非收录日期）
- **链接**：原始来源 URL，必须可访问，卡片整卡可点击跳转（`target="_blank" rel="noopener noreferrer"`）

### 5.3 视觉规范

- 内联 CSS，单文件自包含，不引用外部资源。
- 暗色主题（背景 `#0a0e1a`），卡片 hover 有微动效。
- 顶部 Hero 展示日期、总条数与两版块统计；中部 sticky 锚点导航；正文响应式卡片网格。
- 「论文研究」一级版块内设置二级分段标题和条数统计；导航应能跳转到每个非空研究分段。分段名称直接采用第 6.2 节研究点或第 6.3 节关键词。
- 文末标注总条数与实际使用的数据源（AI HOT + 具体期刊简称 + ArXiv；未收录内容的来源不虚假列入）。
- 同一项目每日文件保持视觉风格一致。
- `index.html` 的 `<body>` 必须保留 `data-template="clinical-dossier-v1"`，并沿用模板中的 `research`、`industry` 固定锚点。
- 沿用 `template.html` 的 hero、source-strip、sticky nav、section、card、evidence、disclaimer、footer 类名与嵌套关系；不得叠加第二套覆盖样式或用成片内联 `style` 重画页面。
- 每日只替换日期、统计、卡片内容、来源、证据等级、时间、链接和页脚数据；设计 token、布局、动效、断点与免责声明结构保持不变。
- `index.html` 中不得残留 `{{...}}` 占位符；同一页面只能有一个主样式块，并保留跳转正文链接、焦点样式和 `prefers-reduced-motion`。

## 6. 数据来源策略（行业源 + 多研究源）

> 行业动态必须检索 aihot；论文研究必须逐一检索第 6.2 节列出的 6 本重点期刊，并检索 ArXiv 作为补充候选。任何来源或分段没有合格内容时允许 0 条收录，必须如实标注并坚持宁缺毋滥。

### 6.1 aihot skill（行业动态）

- 调用 `/api/v1/dailies/latest` 获取日报；若 404 则查 `/api/v1/dailies?limit=7` 取最近日期。
- 调用 `/api/v1/items?mode=all&q=medical&window=24h&limit=20` 和 `q=health` 搜索医疗相关条目。
- 注意：aihot 是通用 AI 资讯平台，医疗内容少。仅保留**直接医疗相关**的条目（含以下关键词：医疗/医学/临床/病毒/病原体/疫苗/疾病/诊断/病理/治疗/手术/肿瘤/基因/超声/PET/CT影像/心理健康/生物安全/临床试验/Isomorphic Labs）。
- 中文关键词在 aihot 几乎无结果，须用英文 medical/health 搜索。

### 6.2 重点期刊 + ArXiv（论文研究）

#### 6.2.1 必查期刊

每次生成都必须分别检索以下 6 本期刊，不得只查 ArXiv，也不得因为某一期刊当日无新文而跳过检索动作：

| 简称 | 期刊全名 | 最终核验来源 |
|---|---|---|
| `TMI` | IEEE Transactions on Medical Imaging | IEEE Xplore 论文页 / DOI |
| `MIA` | Medical Image Analysis | ScienceDirect 论文页 / DOI |
| `JBHI` | IEEE Journal of Biomedical and Health Informatics | IEEE Xplore 论文页 / DOI |
| `NC` | Nature Communications | Nature 官方论文页 / DOI |
| `npj Digital Medicine` | npj Digital Medicine | Nature 官方论文页 / DOI |
| `Nature Biomedical Engineering` | Nature Biomedical Engineering | Nature 官方论文页 / DOI |

- 可使用期刊官网站内检索、PubMed、Crossref 或可靠学术索引发现候选；**最终必须回到出版社论文页或 DOI 落地页**核验标题、作者、机构、摘要、在线发表日期、文章类型与链接。
- 检索每本期刊时，将第 6.2.3 节固定研究点和第 6.3 节研究关键词分别与期刊名组合查询；不得只浏览期刊首页推荐文章。
- 正式卷期论文、Online First、Early Access 与 Article in Press 均可进入候选池，但必须按真实出版状态标注，不得把提前在线论文写成已正式编入卷期。

#### 6.2.2 ArXiv 补充检索

- 用 WebFetch 调用 `http://export.arxiv.org/api/query?search_query=...&sortBy=relevance&sortOrder=descending&max_results=30`（curl 调用可能返回空，必须用 WebFetch）。ArXiv 用于补充尚未正式发表、但方法与证据价值较高的医学 AI 研究；不得以 ArXiv 结果替代 6 本重点期刊的检索。
- ArXiv 检索以相关度排序为主；可另用 `submittedDate` 排序补充候选，但不得因为发布时间新而降低领域匹配标准。

#### 6.2.3 时间窗、研究点与筛选

- 论文检索时间窗固定为执行日前 90 天（约 3 个月），优先选择近 60 天内的高匹配论文；第 61-90 天仅作为补充范围。期刊以首次在线发表日期为准，ArXiv 查询加入对应的 `submittedDate` 起止条件；超过 90 天的论文不收录。
- 某研究点在 90 天窗口内没有合格论文时直接省略该分段，不得继续向更早年份扩展。
- 固定研究点如下，每个研究点独立检索、筛选并形成一个二级分段：

| # | 查询 | 方向 |
|---|------|------|
| 1 | `all:medical AND all:segmentation` | 医学图像分割（CT/MRI/超声/病理/眼底等各模态） |
| 2 | `all:disease AND all:diagnosis AND all:deep AND all:learning` | 疾病诊断（肿瘤/心血管/神经/眼科/病理等） |
| 3 | `all:foundation AND all:model AND (all:medical OR all:clinical OR all:radiology)` | 医学基础模型（预训练/通用医学模型/SAM医学版等） |
| 4 | `all:agent AND (all:clinical OR all:healthcare OR all:medical)` | 医疗Agent与临床智能体 |

- 读取第 6.3 节的每个研究关键词，并为每个关键词单独构造“关键词 + 医学场景/器官/模态/临床任务”的组合查询。禁止只用宽泛关键词后收录通用 AI 论文。
- 固定研究点和研究关键词均为候选方向，不设“每段必须凑够几篇”的硬指标；某分段无高匹配论文就省略。
- 收录硬门槛为：**研究点/关键词直接匹配 + 医学与 AI 双重相关 + 至少一个大学/研究机构可核验**；任一条件不满足即排除。通过硬门槛后，再按方法与证据价值优先、发布时间次要的顺序选择。
- 必须执行图像分割、疾病诊断、医学基础模型三个核心研究点的检索，但只收录真正匹配的结果。
- 同一论文命中多个方向时，只放入最具体、最符合研究目的的一个分段；按 DOI、PMID、文章号、ArXiv ID 与规范化标题联合去重。
- 同一研究存在期刊版、会议扩展版或 ArXiv 预印本时，优先级为：**期刊正式版 / Online First > 会议正式版 > ArXiv 预印本**。只保留信息最完整、版本最新且可核验的一版，并在必要时说明其与预印本的关系。
- **每篇收录论文都必须提取并核验至少一个大学或研究机构**，在来源 chip 中明确标识。无法确认机构、只能猜测机构或只有作者姓名而无 affiliation 证据时，直接排除该论文。
- 证据状态必须区分 `同行评审`、`Online First`、`Early Access`、`Article in Press`、`会议论文` 与 `预印本`；期刊品牌不自动代表临床证据等级，仍需另行标注回顾性研究、前瞻性试验、动物实验或概念验证等研究设计。

### 6.3 研究关键词

- world model（世界模型；仅收录明确的医学或临床应用）
- PET/CT （核医学影像 AI）

### 6.4 谨慎来源（需交叉验证）

- 社交媒体（X/Twitter、微博）：仅作线索，必须回溯到一手来源。
- 二手媒体报道：标注原始出处。

### 6.5 禁止来源

- 无法溯源的截图、转述；标题党、夸大宣传内容；涉及具体患者隐私的信息。

## 7. 质量约束（医学信息特殊性）

- **必须可溯源**：每条信息必须有可访问的原始链接，禁止凭记忆编写。
- **区分证据等级**：预印本 ≠ 同行评审；动物实验 ≠ 人体试验；概念验证 ≠ 临床应用；回顾性研究 ≠ 前瞻性试验。
- **不解读疗效**：不使用"治愈""突破""革命性"等绝对化表述，原文怎么说自己怎么转述。
- **不替代专业建议**：日报仅为信息汇总，不构成医疗建议，底部须声明。
- **利益冲突标注**：涉及商业产品时，标注厂商背景与商业关系。

## 8. 生成流程

### 8.1 标准流程（人工维护）

1. **准备**：读取 `template.html` 与现有 `index.html`，提取旧首页日报日期并确定本次新首页日期
2. **采集**：aihot（日报+medical/health搜索）取行业动态；逐一检索 TMI、MIA、JBHI、Nature Communications、npj Digital Medicine、Nature Biomedical Engineering，并用 ArXiv 补充预印本候选；所有研究源均按第 6.2 节固定研究点和第 6.3 节研究关键词在 90 天时间窗内检索
3. **筛选**：人工判断是否符合"医学 + AI"双重属性，宁缺毋滥
4. **核验**：逐条打开出版社论文页、DOI 落地页或 ArXiv 原文，确认标题、版本、发表状态、日期与事实，并从 affiliation 或论文正文核验大学/研究机构；机构无法确认的论文不收录
5. **策展**：按两个一级版块归类；论文研究再按研究点和关键词分段，按 DOI / PMID / 文章号 / ArXiv ID / 标题联合去重，优先期刊版后撰写完整中文摘要
6. **归档**：新旧日期不同时，按第 4 节规则将当前 `index.html` 完整保存到 `daily/`；归档失败则停止
7. **生成**：归档成功后原位更新 `index.html` 的日期、页面内容与统计
8. **自检**：核对归档存在且未被改写，并检查新首页的领域匹配、研究分段、机构标识、链接、时间、统计和免责声明
9. **入库**：Git 提交时同时纳入 `index.html` 与新增归档

### 8.2 外部 AGENT 约束

- 外部 AGENT 只允许写入按第 4 节生成的 `daily/` 旧页归档和最新页 `index.html`，**数据必须逐条策展与核验**，禁止把未核验内容直接写入首页。
- 第 12 节执行提示词在更新 `index.html` 后即结束，不执行 Git 提交、推送或外部发布；第 9 节仅供人工维护时使用。
- 页面数据结构变更时，同步更新本文件第 5.2 节和后续确认的 HTML 模板规范。

## 9. Git 版本管理

### 9.1 提交前缀

| 前缀 | 用途 | 示例 |
|------|------|------|
| `medical:` | 医学AI日报内容更新 | `medical: 更新医学 AI 日报首页` |
| `feat:` | 模板/脚本结构升级 | `feat: 日报改为两版块结构` |
| `fix:` | 修正内容/链接错误 | `fix: 修正论文链接失效` |
| `chore:` | 规则、配置变更 | `chore: 更新 AGENTS.md 数据源策略` |

### 9.2 提交时机

- 每日收工前提交当日日报。
- 规则或脚本变更随当日提交一起入库。
- 一条提交只做一件事，不把多天改动混在一起。

## 10. 助理工作约束

- **扫描权限**：只允许读取本项目 `MedAI/` 内文件；用户未明确允许的项目外文档不扫描。
- **修改前先读**：编辑任何 HTML 前先读取当前内容，禁止整文件无脑覆盖。
- **不确定时保守处理**：外部 AGENT 无人值守执行时，领域归属或机构信息有疑义就排除该条；仅在交互式维护规则或模板时询问用户，不替用户做未经依据的主观分类。
- **对外动作谨慎**：日报仅在本项目内生成，不主动发布到外部平台。

## 11. 常用命令速查

```bash
# 查看未提交改动
git status
# 更新首页并归档旧版后提交
git add index.html daily/ && git commit -m "medical: 更新医学 AI 日报首页并归档旧版"
# 查看首页历史
git log --oneline --stat -- index.html
# 查看日归档
git log --oneline --stat -- daily/
# 回看某次首页
git show <commit>:index.html
```

## 12. 执行提示词（供外部 AGENT / 调度器使用）

> 以下提示词只负责归档旧首页并生成最新医学 AI 日报，不负责创建或配置任何自动化任务，也不执行 Git 提交、推送或外部发布。

```text
你正在维护「医学 AI 日报（MedAI Brief）」独立项目。按 Asia/Shanghai 当天日期生成医学 AI 晨报，聚焦人工智能 × 医学交叉领域，并严格遵守以下要求。

### 一、开始前

1. 工作范围仅限 `MedAI/`。开始时完整读取 `MedAI/AGENTS.md`，并以其中最新规则为准；不得读取父目录的 `AGENT.md` 或依赖父目录规则、脚本和数据文件。
2. 修改前必须完整读取 `MedAI/template.html` 与现有 `MedAI/index.html`。`template.html` 是只读视觉与 DOM 基准；保留 `data-template="clinical-dossier-v1"`、固定锚点、CSS 类、响应式布局、无障碍属性、有效交互和医学免责声明，只把当天内容与统计写入 `index.html`，不得另起一套样式。
3. 本次只允许写入最新页 `MedAI/index.html`，以及按规则产生的一个旧页归档 `MedAI/daily/YYYY-MM-DD[-HHmmss].html`。允许在缺失时创建 `MedAI/daily/`；不得修改 `MedAI/template.html`，不得在项目根目录创建日期 HTML，不得创建其他归档目录、旧生成脚本或其他文件。
4. 不执行 `git add`、`git commit`、`git push`，不发布到任何外部平台。

### 二、数据源（aihot + 重点期刊 + ArXiv）

行业动态必须检索 aihot；论文研究必须逐一检索 TMI、MIA、JBHI、Nature Communications、npj Digital Medicine、Nature Biomedical Engineering，并检索 ArXiv 作为补充。各来源不要求都收录内容；某来源没有合格条目时允许 0 条，并在交付说明中如实标注，禁止凑数。

#### 1. aihot skill：行业动态

- 调用 `/api/v1/dailies/latest` 获取日报；若当天日报未生成或返回 404，查询 `/api/v1/dailies?limit=7` 并选择最近一期。页面必须如实标注实际数据日期，不得把旧数据伪装成当天数据。
- 分别调用 `/api/v1/items?mode=all&q=medical&window=24h&limit=20` 和 `/api/v1/items?mode=all&q=health&window=24h&limit=20` 搜索医疗相关条目。
- 仅保留同时具有“医学/临床”和“AI/计算”属性的条目，包括医疗、医学、临床、病毒、病原体、疫苗、疾病、诊断、病理、治疗、手术、肿瘤、基因、超声、PET、CT 影像、心理健康、生物安全、临床试验、Isomorphic Labs 等直接相关主题。
- 严禁硬塞通用世界模型、通用 Agent 插件规范、编码工具等无明确医疗应用的 AI 条目。中文关键词在 aihot 中结果较少，必须使用英文 `medical` / `health` 搜索。
- 行业条目必须回到一手来源核验标题、日期、事实和链接；社交媒体或二手报道只能作为线索。

#### 2. 重点期刊与 ArXiv：论文研究

- 每次执行必须分别检索以下期刊：`TMI`（IEEE Transactions on Medical Imaging）、`MIA`（Medical Image Analysis）、`JBHI`（IEEE Journal of Biomedical and Health Informatics）、`NC`（Nature Communications）、`npj Digital Medicine`、`Nature Biomedical Engineering`。不得只搜索 ArXiv。
- TMI/JBHI 最终回到 IEEE Xplore 或 DOI 页核验；MIA 回到 ScienceDirect 或 DOI 页；NC、npj Digital Medicine、Nature Biomedical Engineering 回到 Nature 官方论文页或 DOI 页。PubMed、Crossref 和其他学术索引只能用于发现候选与交叉核对。
- 期刊正式卷期论文、Online First、Early Access、Article in Press 均可进入候选池，但来源 chip 使用实际期刊简称，证据状态如实标注。
- 使用 WebFetch 调用 `http://export.arxiv.org/api/query?search_query=...&sortBy=relevance&sortOrder=descending&max_results=30` 补充预印本候选；不要用可能返回空结果的 curl 代替 WebFetch。ArXiv 不得替代 6 本重点期刊的检索。
- 论文检索时间窗固定为执行日前 90 天（约 3 个月），优先选择近 60 天内的高匹配论文，第 61-90 天仅作为补充范围。期刊以首次在线发表日期为准；ArXiv 查询加入 `submittedDate` 起止条件；超过 90 天的论文不得收录。
- 某研究点或关键词在 90 天窗口内没有高匹配论文时直接省略该分段，不得继续向更早年份扩展；在时间窗内，领域匹配仍然比具体发布时间更重要。
- 分别检索以下固定研究点，每个研究点独立形成一个候选池：
  1. `all:medical AND all:segmentation` — 医学图像分割；
  2. `all:disease AND all:diagnosis AND all:deep AND all:learning` — 疾病诊断；
  3. `all:foundation AND all:model AND (all:medical OR all:clinical OR all:radiology)` — 医学基础模型；
  4. `all:agent AND (all:clinical OR all:healthcare OR all:medical)` — 医疗 Agent 与临床智能体；
- 研究关键词必须读取 `MedAI/AGENTS.md` 第 6.3 节；每个关键词单独形成一个候选池，并使用“关键词 + medical/clinical/器官/影像模态/临床任务”等组合查询保证医学领域匹配。不得读取已废除的父级 `AGENT.md`。
- 固定研究点和每个研究关键词都必须作为独立论文分段。某分段没有高匹配论文时直接省略，不得拿相邻领域或通用 AI 论文凑数。
- 同一论文命中多个候选池时，只归入最具体、最符合研究目的的一个主分段，并按 DOI、PMID、期刊文章号、ArXiv ID 与规范化标题联合去重。
- 同一研究同时存在期刊版、会议扩展版和 ArXiv 预印本时，优先收录期刊正式版 / Online First，其次会议正式版，最后才是 ArXiv 预印本；不得重复展示。
- 收录硬门槛为：研究点/关键词直接匹配 + 医学与 AI 双重相关 + 至少一个大学/研究机构可核验；任一条件不满足即排除。通过硬门槛后，再按方法与证据价值优先、发布时间次要的顺序选择。
- 从作者 affiliations、出版社论文页、论文 HTML/PDF 或可信的一手页面核验所属大学/研究机构。每篇收录论文必须确认至少一个机构；期刊论文来源写成 `期刊简称 · 大学/研究机构名`，预印本写成 `ArXiv · 大学/研究机构名`。多机构合作可列 1-3 个关键机构。无法可靠确认机构时直接排除，不得猜测或写“机构未公开”。

### 三、归档旧首页

1. 新首页的日报日期使用本次执行时的北京时间日期；aihot 数据日期与论文实际发布日期必须另行如实标注，不得与首页日期混淆。
2. 从当前 `MedAI/index.html` 的 `<title>`、Hero 或明确日期字段中提取旧首页自身的日报日期。无法可靠提取时立即停止，不得猜测日期或覆盖首页。
3. 旧首页日期与新首页日期不同时，先把当前 `MedAI/index.html` 的完整内容原样保存为 `MedAI/daily/<旧首页日期>.html`，归档成功后才允许更新 `MedAI/index.html`。
4. 若目标归档已存在且内容完全一致，保留原文件并视为归档完成；若内容不同，禁止覆盖已有归档，将当前旧首页保存为 `MedAI/daily/<旧首页日期>-HHmmss.html`，后缀使用归档时的北京时间。若带后缀路径仍重名，重新取当前北京时间生成唯一文件名；仍冲突则停止，绝不覆盖。
5. 旧首页日期与新首页日期相同时属于同日修订，不新增归档文件，直接更新当前首页。
6. 归档必须保留旧页完整 HTML/CSS/JS、医学免责声明和旧内容，不得套用新数据、不得修改 `MedAI/template.html`。归档写入失败时停止任务并保持 `MedAI/index.html` 不变。

### 四、筛选、摘要与证据纪律

1. 只保留与医疗、医学、生物或健康直接相关，且明确使用 AI/机器学习方法的内容。目标总数为 15–25 条，但领域匹配优先于数量和时效，宁缺毋滥。
2. 仅保留两个一级版块并按此顺序展示：论文研究、行业动态。「论文研究」内部必须依次按第 6.2 节固定研究点和第 6.3 节研究关键词设置二级分段；若某分段或版块无合格条目则省略，不得伪造或用通用 AI 内容补位。
3. 所有英文论文摘要必须基于原文完整翻译和改写为流畅中文，不直接粘贴英文 abstract，不截断关键方法、数据、结果与局限。
4. 每条论文同时标注发表状态与证据阶段，例如“同行评审 / Online First / Article in Press / 预印本”和“回顾性研究 / 前瞻性试验 / 动物实验 / 概念验证”；期刊发表不等同于已经临床验证。
5. 不使用“治愈”“革命性突破”等超出证据的绝对化措辞。涉及商业产品时标明厂商背景或商业关系；不得收录患者隐私信息。
6. 每条内容必须有可访问、可追溯的原始链接；不得凭记忆编写事实。

### 五、首页内容与视觉

1. 条目跨版块和论文二级分段全局连续编号，不在任何分段内重新计数。
2. 更新 `<title>`、Hero 日期、数据覆盖说明、总条数、论文研究与行业动态统计、sticky 锚点导航、正文和页脚，确保所有数字一致。论文研究还要显示每个非空研究分段的名称、条数和可跳转锚点。
3. 每张论文卡片包含：序号；研究分段标签；清晰标题；`期刊简称/ArXiv · 大学/研究机构名` 来源 chip；完整中文摘要；发表状态与证据等级；实际在线发布日期；原文链接。行业动态卡片使用原始来源名。
4. 论文发布日期较近时使用今天/昨天/N 天前；较早论文使用 `YYYY年M月D日`，不得为追求“今日感”隐藏真实年份。
5. 所有外链使用 `target="_blank" rel="noopener noreferrer"`，并确保原文 URL 可访问。
6. 保持现有 MedAI Brief 暗色临床简报主题（`#0a0e1a` 背景）、卡片 hover 微动效、sticky 导航和响应式卡片网格；纯 HTML/CSS/JS 单文件，样式与脚本全部内联，无 CDN、外部字体或外部图片资源。
7. 页脚标注总条数与本期实际收录的数据源（AI HOT + 具体期刊简称 + ArXiv），并完整保留“仅供信息汇总，不构成医疗建议”的免责声明。
8. 严格沿用 `MedAI/template.html` 的单一主样式块和 DOM 类名，固定使用 `research`、`industry` 锚点；不得残留 `{{...}}` 模板占位符。

### 六、完成前检查与交付

1. 确认写入范围仅包括应有的 `MedAI/daily/` 旧页归档和 `MedAI/index.html`；两者均为 UTF-8，HTML 结构完整。
2. 核对 Hero、导航、两个一级版块、论文二级分段、全局编号、卡片数量与页脚统计一致。
3. 检查所有分段锚点、原文 URL、`target`、`rel`、DOI/文章身份、真实在线发布日期和机构标识；页面不得直接展示 ISO 时间字符串。
4. 逐条确认研究点/关键词匹配、医学 + AI 双重相关性、机构证据、中文摘要忠实性、发表状态、证据等级和免责声明；确认期刊版与预印本没有重复；任一论文机构无法核验时删除该条。
5. 最终回复简要说明当天重点、总条数、各论文研究分段数量、行业动态数量、论文时间范围、实际收录的期刊与 ArXiv 数量、新首页路径 `MedAI/index.html`，以及本次归档路径；同日修订未归档时明确说明。不要声称已提交、推送或发布。
```

## 13. 项目演进记录

| 日期 | 版本 | 变更 |
|------|------|------|
| 2026-08-07 | 1.0 | 项目初始化，四区块结构 |
| 2026-08-07 | 2.0 | 改为两版块（论文研究+行业动态）；摘要完整显示；论文标识大学/机构；卡片显示发布日期；数据源明确双源（aihot+ArXiv 6方向含动态关键词）；固化执行提示词 |
| 2026-08-07 | 3.0 | 废除父项目、daily 与日期文件管理，改为独立项目固定维护 index.html |
| 2026-08-07 | 3.1 | 重写供外部 AGENT 使用的执行提示词，补齐文件边界、证据等级、机构核验与交付自检 |
| 2026-08-07 | 3.2 | 论文按固定研究点与研究关键词分段；扩大检索年份并改为领域匹配优先；大学/研究机构改为必填核验项 |
| 2026-08-07 | 3.3 | 论文检索时间窗调整为最近 2-3 个月（最多 90 天），窗口内无合格结果时不向更早年份扩展 |
| 2026-08-07 | 3.4 | 新增只读 `template.html` 视觉基准、稳定 DOM 契约与论文二级分段模板 |
| 2026-08-07 | 3.5 | 恢复 `daily/` 历史归档；生成新一期前按旧首页日期归档，最新一期仍固定写入 `index.html` |
| 2026-08-07 | 3.6 | 论文源扩展为 TMI、MIA、JBHI、Nature Communications、npj Digital Medicine、Nature Biomedical Engineering 必查，ArXiv 改为补充；增加跨版本去重与期刊版优先规则 |

---
_版本：3.6 · 2026-08-07 当前首页 + 日归档版本：`index.html` 展示最新一期，`daily/` 保存旧页；论文研究采用 6 本重点期刊必查 + ArXiv 补充，并保留分段、90 天时间窗、机构必填与只读视觉模板_
